import { useEffect } from 'react';

export function useFrameworkReady() {
  useEffect(() => {
    // Framework ready logic for React Native
    // This can be used for any initialization needed when the app is ready
  }, []);
}